# v0.9.5 Release Checklist (Autonomy Infrastructure)

This release follows `v0.9.4` and focuses on reliable autonomous task execution.

Deferred capability governance follows
`docs/architecture/deferred-capability-policy.md`.

## Current Prep Snapshot (2026-03-06)

- Continuity-first inference budget tuning landed in routing (`interactive` and `delegated` paths).
- Runtime fallback chain reordered for continuity under transient provider/network failures.
- Live source behavior soak is green after tuning:
  - `BASE_URL=http://127.0.0.1:18789 SOAK_TIMEOUT_SECONDS=300 just test-agent-behavior-soak`
  - Report: `/tmp/ironclad-agent-behavior-soak-report.json`
- Guardrail hardening now strips internal tool/delegation protocol leakage in both live and cache-hit output paths.
- Primary runtime model is currently pinned operationally to Moonshot in local runtime config to avoid OpenAI quota-driven fallback churn.
- Scheduler reliability hardening landed:
  - New cron jobs now default to a valid executable payload (`action=log`) instead of inert/unknown-action payloads.
  - Scheduler calendar now projects planned occurrences (not only historical runs), so new jobs are visible before first execution.
  - Job intent is now persisted as first-class cron `description` (API + UI), and `ironclad schedule list` surfaces intent directly.
  - Existing-job updates are idempotent (description-only/no-op updates return success for existing jobs).
- Behavior soak coverage expanded with explicit regressions for:
  - false filesystem capability denials,
  - subagent capability-answer quality.
- Internal protocol sanitization now fails closed into deterministic user-facing fallbacks (no leaked protocol placeholders).
- Execution shortcuts now include deterministic markdown-file count handling (including strict numeric-only mode when requested).
- Latest source soak run: 15/15 passing scenarios (`scripts/run-agent-behavior-soak.py`).
- Latest fast CI gate: green (`just ci-test-fast`, duration ~14m).

## Scope

- `2.4` Speculative Execution
- `1.19` Browser External Runtime
- `1.24` CLI Agent Skills (Claude Code + Codex)
- `2.18` Revenue Autonomy Scaffold + Phase 1 paid-service loop
- Duncan behavior polish cycle (response quality + continuity hardening)
- Revenue Phase 2 shared control-plane primitives (intake + qualification gate)

## Execution Order

### Phase 0: Safety & Control Plane

0a. **Autonomy policy envelope**

- Define explicit policy boundaries for speculative tool execution and external runtimes.
- Ensure all autonomy paths emit auditable policy decisions.

0b. **Execution budget guards**

- Add hard limits for parallel branches, runtime duration, and external tool budget.
- Ensure deterministic cancel/cleanup semantics.

### Phase 1: `2.4` Speculative Execution

1a. **Branch planner + scorer integration**

- Enable multi-branch candidate execution for high-latency turns.
- Score branches and commit best result under policy and budget constraints.

1b. **Branch cancellation and result safety**

- Cancel losing branches cleanly.
- Ensure side-effecting tools are never speculatively executed without explicit safe mode.

### Phase 2: `1.19` Browser External Runtime

2a. **External runtime abstraction**

- Implement runtime boundary for browser operations outside core process.
- Add lifecycle management, health checks, and failure recovery.

2b. **Security and sandbox controls**

- Enforce URL/domain, file access, and capability restrictions.
- Add operator-facing diagnostics for blocked/allowed actions.

### Phase 3: `1.24` CLI Agent Skills

3a. **CLI skill adapter interfaces**

- Add typed adapters for Claude Code and Codex CLI execution as skills.
- Normalize request/response format and tool metadata.

3b. **Credential and environment isolation**

- Ensure per-skill environment scoping and secret handling.
- Add timeout/retry/error classification for CLI-backed skills.

### Phase 4: Integration & Operationalization

4a. **Cross-feature integration tests**

- Validate speculative + browser + CLI skill interactions under budget/policy pressure.

4b. **Operator controls**

- Add enable/disable toggles and diagnostics for each autonomy subsystem.

### Phase 5: `2.18` Revenue Autonomy Scaffold

5a. **Control-plane scaffold**

- Implement one canonical lifecycle for revenue work (intake, gate, score, plan, fulfill, settle, feedback).
- Ensure all revenue strategies reuse this control plane without bypasses.

5b. **Phase 1 paid-service loop**

- Ship one paid service (`geopolitical-sitrep-verified`) end-to-end.
- Wire quote -> payment verification -> fulfillment -> ledger settlement with persistent DB records.

### Phase 6: Deferred-Capability Governance + Phase 2 Revenue Continuation

6a. **Deferred capability policy operationalization**

- Link release flow to deferred capability policy.
- Record out-of-scope capabilities explicitly to prevent reintroduction drift.
- Keep Conway integration explicitly deferred and non-operational for v0.9.5.

6b. **Revenue Phase 2 control-plane continuation**

- Add opportunity intake + qualification gate primitives reused by all revenue strategies.
- Implement first micro-bounty adapter against shared lifecycle (no bespoke path).
- Add mechanic probe/reconcile/orphan-repair hooks for revenue control-plane health.

## Release Gates

### Core Gates

- [ ] Speculative execution never bypasses policy/approval constraints
- [x] Side-effecting speculative branches are blocked or explicitly guarded
- [ ] Browser external runtime recovers from crash/disconnect paths
- [ ] CLI skill adapters produce stable typed outputs and error classes
- [ ] Global autonomy budget guards enforce max concurrency and duration
- [ ] `just ci-test` passes with no coverage regression from prior baseline
- [x] Paid-service flow enforces valid status transitions (`quoted -> payment_verified -> completed`)
- [x] Revenue ledger contains auditable `service_revenue` entries for paid requests
- [x] Deferred capability policy is linked and enforced for v0.9.5 execution
- [x] Conway remains deferred with no vestigial runtime wiring in active scope
- [x] Revenue intake/gate lifecycle is shared and strategy-agnostic
- [x] Capability-to-skill parity gate passes for shipped runtime capabilities

### Reliability Gates

- [ ] Cancellation/cleanup tests pass for speculative branch losers
- [ ] Browser runtime sandbox restrictions are covered by tests
- [ ] CLI skill isolation tests confirm env/secret boundaries
- [x] Operator diagnostics surface autonomy decisions and failures clearly

## Documentation Gate

- [x] `docs/releases/v0.9.5.md` maintained with final shipped scope
- [x] `docs/ROADMAP.md` status updated for `2.4`, `1.19`, `1.24` slices
- [x] Architecture docs updated for autonomy control/data flow
- [x] Revenue scaffold spec maintained in `docs/architecture/revenue-autonomy-scaffold.md`
- [x] Deferred capability policy maintained in `docs/architecture/deferred-capability-policy.md`
- [x] `registry/builtin-skills.json` reflects shipped capability coverage
- [x] `CHANGELOG.md` entry for v0.9.5

## Verification Commands

```bash
just ci-test
cargo test -p ironclad-server
cargo test -p ironclad-agent
cargo test -p ironclad-browser
cargo test -p ironclad-tests -- autonomy
```
