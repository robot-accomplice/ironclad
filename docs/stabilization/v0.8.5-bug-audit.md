# v0.8.5 Bug Audit

Generated 2026-02-28. Three parallel scans: security/correctness, silent failures, smoke/UAT battery.

---

## Scan 1: Security & Correctness (12 findings)

### CRITICAL

| ID | File | Lines | Category | Description |
|----|------|-------|----------|-------------|
| BUG-101 | `agent/src/wasm.rs` | 167-183 | resource exhaustion | WASM plugin timeout is post-hoc only — `process()` runs to completion, elapsed time checked afterward with only a `warn!()`. A malicious WASM module can run indefinitely. |
| BUG-102 | `agent/src/script_runner.rs` | 62-78 | resource exhaustion | Script runner timeout doesn't `kill()` the child process. `tokio::time::timeout` fires but the spawned OS process continues as an orphan. |

### HIGH

| ID | File | Lines | Category | Description |
|----|------|-------|----------|-------------|
| BUG-103 | `server/src/rate_limit.rs` | 106-115 | resource exhaustion | Per-IP and per-actor rate limit HashMaps grow unbounded within a time window. Many distinct IPs create entries only evicted on window reset. |
| BUG-104 | `agent/src/knowledge.rs` | 114 | resource exhaustion | `DirectorySource::query()` calls `read_to_string` with no size limit. A large file in the knowledge directory causes OOM. |
| BUG-105 | `agent/src/knowledge.rs` | 216, 339 | panic | `reqwest::Client::builder().build().expect()` can panic if TLS backend fails to initialize (minimal containers). |
| BUG-106 | `server/src/api/routes/admin.rs` | 315-351 | input validation | Config endpoint uses a blocklist to strip secret fields. New secret fields leak by default. Should be allowlist. |

### MEDIUM

| ID | File | Lines | Category | Description |
|----|------|-------|----------|-------------|
| BUG-107 | `server/src/api/routes/interview.rs` | 9, 37-47 | resource exhaustion | Interview session conversation history (`Vec<String>`) grows unbounded per session within the 3600s TTL. |
| BUG-108 | `server/src/main.rs` | 1590 | panic | Signal handler installation uses `.expect()` — will crash the server on containerized environments where signal setup fails. |
| BUG-109 | `schedule/src/heartbeat.rs` | 51 | logic error | `interval_for_tier()` has `_ => unreachable!()` catch-all — adding a new `SurvivalTier` variant compiles but panics at runtime. |
| BUG-110 | `agent/src/obsidian.rs` | 159 | resource exhaustion | `parse_note()` calls `read_to_string` on every markdown file with no size limit during vault scan. |
| BUG-111 | `agent/src/obsidian.rs` | 598, 613 | performance/panic | `Regex::new().expect()` called on every invocation instead of `LazyLock` static. |
| BUG-112 | `agent/src/spawning.rs` | 174-195 | logic error | `record_spending()` uses f64 arithmetic for budget tracking — floating-point precision errors in financial enforcement. `Money` type exists but isn't used here. |

---

## Scan 2: Silent Failures & Error Handling (38 findings)

### CRITICAL

| # | File | Lines | Pattern | Impact |
|---|------|-------|---------|--------|
| SF-15 | `server/src/api/routes/subagents.rs` | 316-317 | `let _ =` on register/start | Agent creation API returns success but agent never starts |
| SF-16 | `server/src/api/routes/subagents.rs` | 412-417 | `let _ =` on register/start/stop/unregister | Agent update API returns success while runtime state is inconsistent |
| SF-17 | `server/src/api/routes/subagents.rs` | 432-433 | `let _ =` on stop/unregister | Agent delete API returns success but agent remains running as orphan |
| SF-18 | `server/src/api/routes/subagents.rs` | 480-485 | `let _ =` on register/start | PATCH endpoint repeats same silent failure pattern |
| SF-19 | `server/src/api/routes/agent.rs` | 3600-3601 | `let _ =` on register/start | Specialist agent creation silently fails in delegation flow |
| SF-20 | `server/src/api/routes/agent.rs` | 3815 | `let _ =` on assign_agent | Workflow task assignment silently discarded |
| SF-21 | `server/src/lib.rs` | 185 | `.unwrap_or_default()` on env var | API key resolution returns empty string — causes cryptic 401s |
| SF-22 | `server/src/lib.rs` | 408 | `.unwrap_or_default()` on env var | Email password returns empty string — conflates "not set" with "set to empty" |

### HIGH

| # | File | Lines | Pattern | Impact |
|---|------|-------|---------|--------|
| SF-23 | `server/src/api/routes/agent.rs` | 440, 960, 3257, 3453 | `.unwrap_or_default()` on `list_sub_agents` | DB errors silently treated as "no agents" at 4 sites — delegation decisions made on stale/empty data |
| SF-24 | `server/src/api/routes/skills.rs` | 253 | `.unwrap_or_default()` on `list_skills` | DB failure during skill sync treated as "no skills" — risk of duplicates or lost customizations |
| SF-25 | `server/src/lib.rs` | 535 | `let _ =` on MCP discover | MCP client discovery failure silently ignored at startup — agent boots with no MCP tools |
| SF-26 | `server/src/lib.rs` | 655 | `.unwrap_or_default()` on `load_cache_entries` | Semantic cache fails to load — lifetime of cached entries silently lost |
| SF-27 | `server/src/api/routes/agent.rs` | 2455 | `.unwrap_or_default()` on `resolve_api_key` | API key resolution returns empty string — LLM 401 with no context |
| SF-28 | `ironclad-core/src/config.rs` | 342 | `.unwrap_or_default()` on `toml::from_str` | Bundled providers TOML parse failure silently produces empty provider map |
| SF-29 | `server/src/config_runtime.rs` | 254 | `let _ =` on `restore_from_backup` | Failed hot-reload backup restoration silently discarded — config file left corrupted |
| SF-30 | `server/src/migrate/transform.rs` | 1106, 1358, 1579 | `let _ =` on SQL execute | Migration operations silently discard SQL errors — partial data import |
| SF-31 | `server/src/api/routes/agent.rs` | 2936-2957 | `let _ =` on `send_ephemeral` | Channel thinking indicators silently fail across all 4 platforms |
| SF-32 | `server/src/api/routes/sessions.rs` | 237, 275 | `.unwrap_or_default()` on JSON deser | Model selection candidates silently return empty list |
| SF-33 | `channels/src/telegram.rs` | 243, 265 | `let _ =` on HTTP POST | Typing and delete silently discard Telegram API errors |

### MEDIUM

| # | File | Lines | Pattern | Impact |
|---|------|-------|---------|--------|
| SF-34 | `server/src/api/routes/subagents.rs` | 184 | `.unwrap_or_default()` on DB | Session counts return 0 on DB error |
| SF-35 | `server/src/api/routes/agent.rs` | 432 | `.unwrap_or_default()` on JSON | Malformed subtask JSON silently produces empty task list |
| SF-36 | `ironclad-llm/src/oauth.rs` | 236 | `let _ =` on `set_permissions` | OAuth token file may remain world-readable |
| SF-37 | `ironclad-plugin-sdk/src/script.rs` | 275, 284 | `.ok()` on `read_to_end` | Plugin stdout/stderr read failure silently returns empty |
| SF-38 | `agent/src/retrieval.rs` | 110 | `.ok()?` on `retrieve_working` | Working memory retrieval silently returns None on DB error |
| SF-39 | `agent/src/digest.rs` | 20 | `.ok()?` on `list_messages` | Episodic digest silently aborts on DB error |
| SF-40 | `server/src/api/routes/skills.rs` | 555-576 | `let _ =` on fs write/remove | Skill install rollback silently fails — inconsistent state |
| SF-41 | `server/src/api/routes/agent.rs` | 3806, 3813 | `.unwrap_or_default()` on JSON | Skills JSON parse failure silently returns empty list |
| SF-42 | `channels/src/signal.rs` | 168 | `let _ =` on `json_rpc` | Signal typing indicator silently fails |
| SF-43 | `channels/src/discord.rs` | 137 | `let _ =` on HTTP POST | Discord typing indicator silently fails |
| SF-44 | `channels/src/whatsapp.rs` | 267 | `let _ =` on `mark_as_read` | WhatsApp read receipt silently discarded |
| SF-45 | `ironclad-db/src/sessions.rs` | 129, 152 | `.ok()` on `query_row` | Session upsert treats DB errors same as "no rows" |
| SF-46 | `server/src/cli/defrag.rs` | 533, 591 | `.unwrap_or_default()` on `read_to_string` | Defrag treats unreadable files as empty — misleading reports |
| SF-47 | `ironclad-llm/src/embedding.rs` | 141, 147 | `.unwrap_or_default()` on env var | Vertex AI/Bedrock API key paths still have empty-string fallback |
| SF-48 | `server/src/migrate/transform.rs` | 1635-1891 | `.unwrap_or_default()` cascade | Migration export chains silent failures — partial data loss |
| SF-49 | `server/src/main.rs` | 1686 | `.unwrap_or_default()` on response | CLI response parsing failure returns empty string |
| SF-50 | `ironclad-core/src/keystore.rs` | 265 | `let _ =` on `set_permissions` | Keystore audit log may remain world-readable |
| SF-51 | `server/src/api/routes/sessions.rs` | 454, 489, 559, 668 | `.unwrap_or_default()` on DB reads | Additional session detail endpoints still return defaults on error |
| SF-52 | `agent/src/obsidian.rs` | 148, 170, 314, 703 | `.unwrap_or_default()` | Obsidian vault operations silently swallow errors |

---

## Scan 3: Smoke & UAT Battery

| Check | Result |
|-------|--------|
| Unit + integration tests (3,745) | **PASS** — 0 failures |
| Clippy | **PASS** — 0 warnings |
| Release build warnings | **PASS** — 0 warnings |
| Integration tests (198) | **PASS** — 0 failures |
| Dead code / unused imports | **PASS** — 0 warnings |
| TODO/FIXME markers | 2 intentional TODOs (oauth encryption, manifest permissions) |
| Unsafe blocks | 1 production `unsafe` (`from_utf8_unchecked`, validated) |

---

## Triage Priority

**Fix first (highest impact):**
1. BUG-101 + BUG-102: WASM/script resource exhaustion (CRITICAL — server stability)
2. SF-15 thru SF-20: Sub-agent lifecycle silent failures (CRITICAL — entire delegation system broken on error)
3. SF-21 + SF-22: API key/email env var empty-string fallback (CRITICAL — cryptic auth failures)
4. BUG-103: Rate limiter unbounded memory (HIGH — DoS vector)
5. BUG-104 + BUG-110: Knowledge/Obsidian unbounded file reads (HIGH — OOM)

**Fix next:**
6. SF-23: list_sub_agents at 4 sites
7. SF-24 thru SF-30: Various HIGH silent failures
8. BUG-105: reqwest Client panic
9. BUG-106: Config secret blocklist → allowlist
10. Everything else
